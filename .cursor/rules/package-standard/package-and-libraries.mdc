---
description: Package and library structure conventions (redirect)
alwaysApply: false
---

# Moved

This rule moved to [_thunderstorm/.rules/operational/package-and-libraries.mdc](../../.rules/operational/package-and-libraries.mdc).

Includes the **test-only (E2E)** TypeScript package pattern. Use the [thunderstorm rules index](../index.mdc) for the full routing table.
